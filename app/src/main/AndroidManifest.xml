<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="osp.leobert.android.pandorasample">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".cases.SampleActivity"></activity>
        <activity android:name=".cases.MultiTypeTestActivity" />
        <activity android:name=".menu.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".cases.DataChangeTestActivity" />
        <activity android:name=".cases.DataPropertyChangeTestActivity" />
        <activity android:name=".cases.CompositeOpsInWrapTestActivity" />
        <activity android:name=".kt.TestKtActivity" />
    </application>

</manifest>